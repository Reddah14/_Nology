// function sum(a, b) {
//   return a + b;
// }

// export default sum; // we export so we can import it from outta this file

/* 
  A function that calculated sums
  But I'm doing it TDD
  Start by writing failing tests
  Writing code to pass those tests
  Refactoring code
  Writing more tests
  etc . . . 
*/

export const calculateSum = (num1, num2 ) => num1 + num2;